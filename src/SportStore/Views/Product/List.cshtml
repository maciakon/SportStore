@model ProductListViewModel

@foreach (var p in Model.Products) {
   @Html.Partial("ProductSummary", p)
}

<ul page-model="@Model.PagingInfo" page-actions="List" class="pagination" page-url-category="@Model.CurrentCategory"></ul>